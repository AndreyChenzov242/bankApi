var anim = bodymovin.loadAnimation({
  container: document.getElementById("server"), // Required
  path: "./js/server.json", // Required
  renderer: "svg", // Required
  loop: true, // Optional
  autoplay: true, // Optional
});
